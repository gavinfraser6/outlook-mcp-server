# Development / test dependencies.
# The test-suite uses fakes and does NOT require Windows, Outlook or pywin32,
# so it runs on any platform / CI.
-r requirements.txt
pytest>=8.0
httpx>=0.27   ; python_version >= "3.8"   # required by FastAPI's TestClient
